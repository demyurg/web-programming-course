# L2. React: компоненты и выбор элемента

## Зачем эта тема

На L1 вы создали модель и функции данных. React позволяет превратить эти данные в интерфейс: компонент отвечает за небольшой участок экрана, props передают входы, а state хранит изменяющееся UI-состояние. Важно сохранить доменный слой A1 и добавить поверх него только отображение.

Перед началом откройте существующую папку `trainer/` и убедитесь, что в ней есть ваша версия A1. Подключите React и Vite к этому проекту, сохраните A1 и свои файлы, а поля конфигурации обновите по [гайду](docs/GUIDE.md). Не создавайте проект заново генератором и не теряйте накопленный код.

React и Vite подключаются к тому же проекту точными версиями, выбранными для курса:

```sh
npm install react@19.3.0 react-dom@19.3.0 --save-exact
npm install vite@6.4.3 @vitejs/plugin-react@4.3.4 @types/react@19.3.0 @types/react-dom@19.3.0 --save-dev --save-exact
```

Команды одинаковы в Linux и Windows PowerShell. После установки проверьте `npm run check`, `npm run build` и запуск dev-сервера по гайду. До L6 команда `npm test` не обязательна: тестовый раннер и RTL появятся на отдельном этапе.

## Задание A2

Продолжите A1 в той же папке `trainer/`. Соберите небольшой каталог учебного набора и экран текущего задания, не меняя смысл уже написанных типов и функций.

### Обязательная структура

- компонент списка получает массив заданий и callback выбора через props;
- отдельный компонент одного задания получает один объект через props и показывает `prompt`, `topic` и необязательный `code`;
- для `single-choice` отображаются `options`, а для `short-text` — понятное сообщение о текстовом типе без поля ввода;
- заголовок показывает название набора и количество заданий;
- родитель хранит строковый id выбранного задания в одном `useState` и передаёт необходимые данные дочерним компонентам;
- у элементов списка используется стабильный `task.id` в `key`, а не индекс массива.

Имена компонентов и внутреннюю структуру определите самостоятельно. Данные могут оставаться локальным типизированным набором A1.

### Сценарии проверки

1. При старте виден список и одно выбранное задание.
2. Нажатие на стандартную кнопку меняет карточку без перезагрузки.
3. Обе формы задания отображаются безопасно: варианты читаются только у `single-choice`.
4. Значение `code` показывается как текст и не исполняется.
5. Если выбранный id отсутствует, виден нейтральный пустой экран, а не ошибка доступа к отсутствующему объекту.
6. Узкое окно остаётся читаемым, а стандартные элементы доступны с клавиатуры.
7. Функции A1 по-прежнему проходят `npm run check` и демонстрацию.

### Границы

Кнопка выбора меняет только текущий элемент. Не добавляйте поля ответов, `onChange` ответов, формы, API, сохранение, `useReducer` или сторонние библиотеки. Названия компонентов, данные и внутреннюю структуру определите самостоятельно как продолжение A1.

**Вопросы:** Кто владеет состоянием выбора? Как карточка получает данные? Почему `key` должен быть стабильным? Что происходит при отсутствии id? Как показать `code` безопасно?

## Проверка и сдача

Из `trainer/`, одинаково в Linux и Windows PowerShell:

```sh
npm run check
npm run build
npm run dev
```

Остановите dev-сервер после ручной проверки. A2 сдаётся на P3; предыдущая работа A1 должна оставаться в той же ветке или в основании новой ветки.

## Критерии зачёта

- проект продолжает A1, а React и Vite добавлены в существующую папку;
- данные действительно проходят через props;
- список и карточка разделены по ответственности;
- `kind` сужается перед чтением полей конкретной формы;
- `useState` меняет выбранный id без дублирования всего объекта;
- `task.id` используется как стабильный `key`;
- отсутствующий id и `code` обрабатываются безопасно;
- `npm run check` и `npm run build` проходят.

Материалы: [гайд](docs/GUIDE.md), [краткая справка](docs/CHEATSHEET.md), [интерактивные примеры](docs/interactive.html), [слайды](docs/slides-standalone/slides.html). Все объяснительные примеры в них используют каталог книг, а не предметную область тренажёра.
